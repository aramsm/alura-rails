	<% if flash[:notice] %>
		<div class="alert alert-success" role="alert">
			<%= flash[:notice] %>
			<button type="button" class="close" data-dismiss="alert" aria-label="Close">x</button>
		</div>
	<% end %>
	<table class="table table-bordered">
		<thead>
			<tr>
				<th>Nome</th>
				<th>Descrição</th>
				<th>Preço</th>
				<th>Quantidade</th>
				<th></th>
			</tr>
		</thead>
		<tbody>
		   <%= render @produtos_por_nome %>
		</tbody>
	</table>
	<%= button_to "Adicionar", new_produto_path, method: :get, class: "btn btn-primary" %>
